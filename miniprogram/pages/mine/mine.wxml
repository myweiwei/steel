<!--pages/mine/mine.wxml-->
<view class='my'>
<!-- <button open-type='getUserInfo' lang="zh_CN" bindgetuserinfo="onGotUserInfo">aaa</button> -->
  <view class='top'>
    <!-- <van-cell-group>
      <van-field
        value="{{ value }}"
        placeholder="请输入充值金额"
        border="{{ false }}"
        bind:change="onChange"
      />
    </van-cell-group>
    <van-button type="default" bindtap='addPrice'>充值</van-button> -->
    <view class='flexStart'>
      <image class='user' src='../../images/my/my_23.png'></image>
      <view>
        <view class='flexStart'>
          <text class='name'>aa</text>
            <!-- <image  class='sm' src='../../images/my/my_03.png'></image>
            <text class='smbj'>已实名</text> -->
        </view>
        <view class='flexStart' style='margin-top:10rpx'>
          <text class='name1'>余额(元)****</text>
            <image class='sm1' src='../../images/my/my_07.png'></image>
        </view>
      </view>
    </view>
  </view>
  <view class='bottom'>
    <view class='bottomBox flexCenter'>
      <view wx:for='{{iconList}}' wx:key='{{icon}}' class='lilist {{item.icon}}' >
        <view class='image'>
          <image src='../../images/my/{{item.icon}}.png'></image>
        </view>
        <text>{{item.name}}</text>
      </view>
    </view>
  </view>
</view>
<van-popup
  show="{{ show }}"
  closeable
  close-icon="close"
  position="bottom"
  custom-style="height: 70%"
  bind:close="onClose"
>
  <view>
    <view style='margin-top:80rpx;height:100rpx;'>
      <van-field
          value="{{ newvalue }}"
          type="textarea"
          placeholder="请输入头条信息"
          autosize
          border="{{ false }}"
        />
    </view>
    <view class='label'>选择充值天数：</view>
    <view class='price'>
      <view wx:for='{{priceList}}' class='{{activeIndex==index?"active":""}}' data-index='{{index}}' bindtap="choosePrice">{{item}}</view>
    </view>
    <view class='footer'>
      <van-button color="linear-gradient(to right, orange, #f7d501)" round size='large'>提交</van-button>
    </view>
  </view>
</van-popup>